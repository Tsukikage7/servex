# 应用服务各微服务容器
# 依赖：需先启动 infra/docker-compose.yaml
version: "3.9"

# 复用基础设施网络
networks:
  default:
    name: {{.Name}}-network
    external: true

services:
  # 示例：添加你的微服务
  # example:
  #   build:
  #     context: ../../../
  #     dockerfile: services/example/Dockerfile
  #   container_name: {{.Name}}-example
  #   environment:
  #     - CONFIG_PATH=/app/configs/config.yaml
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - consul
  #     - redis
  #   restart: unless-stopped
