package main

import (
	"fmt"
	"os"
)

func main() {
	app, cleanup, err := initApp()
	if err != nil {
		fmt.Fprintf(os.Stderr, "初始化失败: %v\n", err)
		os.Exit(1)
	}
	defer cleanup()

	if err := app.Run(); err != nil {
		fmt.Fprintf(os.Stderr, "应用运行错误: %v\n", err)
		os.Exit(1)
	}
}
