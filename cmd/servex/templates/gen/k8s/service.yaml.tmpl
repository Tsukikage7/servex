apiVersion: v1
kind: Service
metadata:
  name: {{.Name}}
  labels:
    app: {{.Name}}
spec:
  type: ClusterIP
  selector:
    app: {{.Name}}
  ports:
    - port: {{.Port}}
      targetPort: {{.Port}}
      protocol: TCP
      name: http
