# {{ .Name }}

AI service scaffold generated by servex.

This service shows how to connect an AI application to the ServeX Go microservice boundary: HTTP API, unified response, LLM provider setup, business agent boundary and local tools.

## Run

```bash
go mod tidy
OPENAI_API_KEY=sk-... just dev
```

```bash
curl -X POST http://localhost:8080/chat \
  -H 'content-type: application/json' \
  -d '{"message":"订单 VOYRA-1001 现在是什么状态？"}'
```

## Choices

- Provider: `{{ .Provider }}`
- Framework: `{{ .Framework }}`

Use `llm/adapter/eino`, `llm/adapter/adk` or a business runtime for complex Agent/Workflow/RAG behavior. servex stays responsible for Go service boundaries, unified responses and production governance.
